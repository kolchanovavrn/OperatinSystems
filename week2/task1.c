#include <stdio.h>
#include <limits.h>
#include <float.h>
int main()
{
    printf("Integer max = %f \n ", INT_MAX);
    printf("Float max = %f \n", FLT_MAX);
    printf("Double max = %f \n ", DBL_MAX);
    return 0;
}
